<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="20dp">

    <TextView
        android:id="@+id/itemFriendsProgressName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="200dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:hint="asdf"
        android:textColor="@color/black"/>

    <ImageView
        android:id="@+id/itemFriendsProgressImageStatus"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_marginStart="10dp"
        app:layout_constraintStart_toEndOf="@+id/itemFriendsProgressName"
        app:layout_constraintTop_toTopOf="@+id/itemFriendsProgressName" />

    <ProgressBar
        android:id="@+id/itemFriendsProgressValue"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:indeterminate="false"
        android:max="100"
        app:layout_constraintStart_toStartOf="@+id/itemFriendsProgressName"
        app:layout_constraintTop_toBottomOf="@+id/itemFriendsProgressName" />

</androidx.constraintlayout.widget.ConstraintLayout>